# Versiones fijadas: una auditoría debe ser reproducible, y un `>=` convierte
# cualquier release upstream en un cambio silencioso del comportamiento del lab.
# Para actualizar: subir el pin conscientemente y contrastar con `make audit`.
#
# pandas se eliminó: ningún módulo lo importa (~50 MB de dependencia muerta).

requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
dnspython==2.7.0
python-whois==0.9.5
aiohttp==3.11.11
tldextract==5.1.3
rich==13.9.4
jinja2==3.1.5
